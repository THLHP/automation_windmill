summary: Database backup script with PostgreSQL 17 client tools
description: >-
  Automatically backs up all databases from a PostgreSQL server using PostgreSQL
  17 client tools. Installs dependencies if needed and provides progress
  tracking.
lock: '!inline f/admin/backup_databases.script.lock'
kind: script
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties:
    backup_directory:
      type: string
      description: ''
      default: null
      originalType: string
    database_credentials:
      type: object
      description: ''
      default: null
      format: resource-postgresql
  required:
    - database_credentials
    - backup_directory
tag: asurc
